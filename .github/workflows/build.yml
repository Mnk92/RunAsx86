name: RunAsx86 build
on: [push, pull_request]
jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
        matrix:
            os: [windows-latest]
    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v2
      with:
        dotnet-version: 6.0.x
    - name: Restore dependencies
      run: dotnet restore
    - name: Build project
      run: dotnet build -c Release --no-restore
    - name: Publish windows
      run: dotnet publish -r win-x86 -c Release --no-restore --no-build
    - name: Run tests
      run: dotnet test -c Release --runtime win-x86 --no-restore --no-build